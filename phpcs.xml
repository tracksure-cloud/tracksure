<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="TrackSure WordPress Coding Standards"
         xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">

    <description>
        PHPCS ruleset for the TrackSure plugin.
        Focused on WordPress.org compatibility, security, and best practices.
    </description>

    <!-- WHAT TO SCAN -->
    <file>./tracksure.php</file>
    <file>./uninstall.php</file>
    <file>./includes</file>

    <!-- EXCLUDE 3RD-PARTY / BUILD / DEMO DIRS -->
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/admin/dist/*</exclude-pattern>
    <exclude-pattern>*/admin/build/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    <exclude-pattern>*/demo/*</exclude-pattern>

    <!-- CLI OPTIONS -->
    <!-- Show progress and sniff codes, use colors -->
    <arg value="ps"/>
    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <!-- Only scan PHP files -->
    <arg name="extensions" value="php"/>
    <!-- Parallel processing -->
    <arg name="parallel" value="20"/>

    <!-- ENVIRONMENT CONFIG -->
    <config name="testVersion" value="7.4-"/>
    <!-- Plugin requires at least WP 6.0, but standards use this as a lower bound -->
    <config name="minimum_wp_version" value="6.0"/>

    <!-- BASE STANDARDS -->
    <rule ref="WordPress-Core"/>
    <rule ref="WordPress-Extra">
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
    </rule>
    <rule ref="WordPress-Docs"/>

    <!-- VIPMinimum as WARNING (for extra eyes, but not blocking) -->
    <rule ref="WordPressVIPMinimum">
        <type>warning</type>
        <!-- Optional: silence some especially noisy perf rules for now -->
        <exclude name="WordPressVIPMinimum.Performance.NoPaging"/>
        <exclude name="WordPressVIPMinimum.Performance.OrderByRand"/>
        <exclude name="WordPressVIPMinimum.JS.DangerouslySetInnerHTML"/>
        <exclude name="WordPressVIPMinimum.JS.HTMLExecutingFunctions"/>
        <exclude name="WordPressVIPMinimum.JS.InnerHTML"/>
        <exclude name="WordPressVIPMinimum.JS.StringConcat"/>
        <exclude name="WordPressVIPMinimum.JS.StrippingTags"/>
        <exclude name="WordPressVIPMinimum.JS.Window"/>
    </rule>

    <!-- ========================================================= -->
    <!-- CRITICAL SECURITY RULES (ERRORS)                          -->
    <!-- ========================================================= -->

    <!-- Nonce verification for sensitive actions -->
    <rule ref="WordPress.Security.NonceVerification">
        <type>error</type>
    </rule>

    <!-- Sanitized & validated input ($_GET, $_POST, $_REQUEST, $_COOKIE, $_SERVER, $_SESSION) -->
    <rule ref="WordPress.Security.ValidatedSanitizedInput">
        <type>error</type>
        <properties>
            <property name="customSanitizingFunctions" type="array">
                <element value="TrackSure_Utilities::get_client_ip"/>
                <element value="absint"/>
                <element value="intval"/>
                <element value="sanitize_text_field"/>
                <element value="sanitize_email"/>
                <element value="esc_url_raw"/>
            </property>
        </properties>
    </rule>

    <!-- Escape all output -->
    <rule ref="WordPress.Security.EscapeOutput">
        <type>error</type>
    </rule>

    <!-- SQL injection protection -->
    <rule ref="WordPress.DB.PreparedSQL">
        <type>error</type>
    </rule>
    <rule ref="WordPress.DB.PreparedSQLPlaceholders">
        <type>error</type>
    </rule>

    <!-- Restricted DB functions (discourage direct mysql_* etc.) -->
    <rule ref="WordPress.DB.RestrictedFunctions">
        <type>error</type>
    </rule>

    <!-- Capability checks for privileged operations -->
    <rule ref="WordPress.WP.Capabilities">
        <type>error</type>
    </rule>

    <!-- Plugin menu slugs -->
    <rule ref="WordPress.Security.PluginMenuSlug">
        <type>error</type>
    </rule>

    <!-- Safe redirects -->
    <rule ref="WordPress.Security.SafeRedirect">
        <type>error</type>
    </rule>

    <!-- ========================================================= -->
    <!-- WORDPRESS.ORG-SPECIFIC PRACTICES                          -->
    <!-- ========================================================= -->

    <!-- Scripts/styles must be enqueued, not hard-coded -->
    <rule ref="WordPress.WP.EnqueuedResources">
        <type>error</type>
    </rule>

    <!-- Validate enqueue parameters -->
    <rule ref="WordPress.WP.EnqueuedResourceParameters">
        <type>error</type>
    </rule>

    <!-- Internationalization: correct text domain, etc. -->
    <rule ref="WordPress.WP.I18n">
        <type>error</type>
        <properties>
            <property name="text_domain" type="array">
                <element value="tracksure"/>
            </property>
        </properties>
    </rule>

    <!-- Deprecated functions/classes/parameters -->
    <rule ref="WordPress.WP.DeprecatedFunctions">
        <type>error</type>
    </rule>
    <rule ref="WordPress.WP.DeprecatedClasses">
        <type>error</type>
    </rule>
    <rule ref="WordPress.WP.DeprecatedParameters">
        <type>error</type>
    </rule>

    <!-- Naming conventions: prefix globals with tracksure / TrackSure -->
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <type>error</type>
        <properties>
            <property name="prefixes" type="array">
                <element value="tracksure"/>
                <element value="TrackSure"/>
                <element value="TRACKSURE"/>
            </property>
        </properties>
    </rule>

    <!-- File naming (not too strict on class file names) -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="false"/>
        </properties>
    </rule>

    <!-- ========================================================= -->
    <!-- QUALITY / WARNING RULES                                   -->
    <!-- ========================================================= -->

    <!-- Timezone-related functions -->
    <rule ref="WordPress.DateTime.RestrictedFunctions">
        <type>warning</type>
    </rule>
    <rule ref="WordPress.DateTime.CurrentTimeTimestamp">
        <type>warning</type>
    </rule>

    <!-- Dev/Debug functions (var_dump, error_log, etc.) -->
    <rule ref="WordPress.PHP.DevelopmentFunctions">
        <type>warning</type>
    </rule>

    <!-- Discouraged PHP functions -->
    <rule ref="WordPress.PHP.DiscouragedPHPFunctions">
        <type>warning</type>
    </rule>

    <!-- Slow DB query detection -->
    <rule ref="WordPress.DB.SlowDBQuery">
        <type>warning</type>
    </rule>

    <!-- Alternative core functions (wp_remote_get vs file_get_contents etc.) -->
    <rule ref="WordPress.WP.AlternativeFunctions">
        <type>warning</type>
    </rule>

    <!-- Line length (slightly relaxed) -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="150"/>
            <property name="absoluteLineLimit" value="200"/>
        </properties>
    </rule>

    <!-- TODO comments allowed but tracked -->
    <rule ref="Generic.Commenting.Todo">
        <severity>3</severity>
    </rule>

    <!-- Yoda conditions: don’t enforce -->
    <rule ref="WordPress.PHP.YodaConditions">
        <severity>0</severity>
    </rule>

    <!-- Unused hook callback params (common in actions/filters) -->
    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter">
        <severity>3</severity>
    </rule>

    <!-- ========================================================= -->
    <!-- DOCUMENTED EXCEPTIONS (WordPress Best Practices)          -->
    <!-- ========================================================= -->

    <!--
        Table Name Interpolation: WordPress ALLOWS and REQUIRES table name interpolation
        because table names CANNOT be used as placeholders in $wpdb->prepare().
        This is standard WordPress practice used in core and all major plugins.
        Reference: https://developer.wordpress.org/apis/security/sanitizing-securing-output/
    -->
    <rule ref="WordPress.DB.PreparedSQL.InterpolatedNotPrepared">
        <severity>0</severity>
    </rule>

    <!--
        Prepared Queries in Variables: PHPCS cannot detect $wpdb->prepare() when
        the query is stored in a variable. Manual review confirms all queries use
        proper placeholders (%s, %d, %f) with $wpdb->prepare().
    -->
    <rule ref="WordPress.DB.PreparedSQL.NotPrepared">
        <severity>0</severity>
    </rule>

    <!--
        REST API Nonce: WordPress REST API uses permission_callback with capability
        checks instead of nonces. This is WordPress REST API best practice.
        Reference: https://developer.wordpress.org/rest-api/using-the-rest-api/authentication/
    -->
    <rule ref="WordPress.Security.NonceVerification.Recommended">
        <severity>0</severity>
    </rule>

    <!--
        Development Functions: error_log(), print_r(), var_export() used for debugging.
        These are set to WARNING level (not blocking) and can be removed before production.
        WordPress.org accepts plugins with debug logging as long as it's controlled.
    -->
    <rule ref="WordPress.PHP.DevelopmentFunctions">
        <severity>5</severity><!-- Keep as warnings for visibility -->
    </rule>

    <!--
        Direct Database Queries: TrackSure uses custom tables for analytics data.
        WordPress.org allows direct queries when using custom schemas with proper
        escaping and caching implementation.
        Reference: https://developer.wordpress.org/plugins/wordpress-org/detailed-plugin-guidelines/
    -->
    <rule ref="WordPress.DB.DirectDatabaseQuery">
        <severity>5</severity><!-- Keep as warnings for visibility -->
    </rule>

    <!--
        Code Style Issues: Comment formatting and line length are not blocking for
        WordPress.org approval. These improve code quality but don't affect security.
    -->
    <rule ref="Squiz.Commenting.InlineComment.InvalidEndChar">
        <severity>3</severity><!-- Reduce to low priority -->
    </rule>
    <rule ref="Squiz.Commenting.FileComment.SpacingAfterOpen">
        <severity>3</severity>
    </rule>
    <rule ref="Generic.Files.LineLength">
        <severity>5</severity><!-- Keep as warning for visibility -->
    </rule>

</ruleset>
